
# Горутины

Легковесные потоки – управляются рантаймом Go, а не ОС (начинается с ~2 КБ, динамически растет).

Запуск – через go func().

Мультиплексирование – планировщик Go распределяет горутины по потокам ОС (M:N).

# Каналы

Типизированные FIFO-очереди для общения между горутинами.

Создание:

```
ch := make(chan int)       // Небуферизованный (синхронный)
chBuff := make(chan int, 3) // Буферизованный (асинхронный)  
```

### буферизированный канал
`c := make(chan int, 1)`

недоступен, пока не будет про

### не буферизированный канал
`var cb chan bool = make(chan bool)`

Операции:

ch <- v – отправить (блокируется, пока данные не будут получены, если небуферизованный).

v := <-ch – получить (блокируется, пока нет данных).

close(ch) – закрыть канал (после этого можно только читать оставшиеся данные).

Селекты:

go
select {
case v := <-ch1: // обработка ch1
case ch2 <- x:   // отправка в ch2
default:         // неблокирующая проверка
}
🔹 Паттерны
Worker Pool – пул горутин для обработки задач.

Fan-out/Fan-in – распределение работы и сбор результатов.

Done-канал – для отмены операций (context.Context предпочтительнее).
